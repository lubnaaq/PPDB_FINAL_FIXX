# 📊 DOCUMENT UPLOAD FLOW & ARCHITECTURE

## 🔄 USER FLOW - Upload Dokumen

```
┌─────────────────────────────────────────────────────────┐
│                    USER DASHBOARD                        │
│                  (Login Required)                         │
└───────────────┬──────────────────────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────────────────────┐
│          CLICK: "Upload Dokumen" Menu                    │
└───────────────┬──────────────────────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────────────────────┐
│     GET /dokumen - DokumenController::index()           │
│     - Load form upload                                   │
│     - Load list dokumen user                             │
└───────────────┬──────────────────────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────────────────────┐
│         SHOW: Upload Form + Document List                │
│  ┌──────────────────────────────────────────────────┐   │
│  │ Nama Dokumen: [Dropdown ▼]                      │   │
│  │ File: [Choose File] [PDF,JPG,PNG - Max 5MB]     │   │
│  │ [Upload Dokumen Button]                         │   │
│  └──────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────┐   │
│  │ Daftar Dokumen Sudah Diupload:                  │   │
│  │ [TABLE] | Download | Delete (jika pending)      │   │
│  └──────────────────────────────────────────────────┘   │
└───────────────┬──────────────────────────────────────────┘
                │
                ▼ [User select dokumen + file]
                │
                ▼ [Click Upload]
                │
    ┌───────────┴────────────┐
    │                        │
    ▼ VALID                  ▼ INVALID
┌──────────────┐       ┌──────────────┐
│POST /dokumen │       │Show Error    │
│              │       │Validation    │
│Validate:     │       │  
│- File type   │       │- File type?  │
│- File size   │       │- File size?  │
│- Field req   │       │- Required?   │
└──────┬───────┘       └──────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────┐
│ DokumenController::store()                              │
│ - Validate file                                         │
│ - Store file: storage/app/public/dokumen/...            │
│ - Create DB record: dokumens table                       │
│ - Set status: "pending"                                 │
└───────────────┬──────────────────────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────────────────────┐
│ RESPONSE: {"success": true, "message": "Uploaded"}      │
└───────────────┬──────────────────────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────────────────────┐
│ RELOAD PAGE: Show in documents list                     │
│ Status: [Menunggu Verifikasi] 🕐                        │
└─────────────────────────────────────────────────────────┘
```

---

## 👨‍💼 ADMIN FLOW - Verifikasi Dokumen

```
┌─────────────────────────────────────────────────────────┐
│                   ADMIN DASHBOARD                        │
│                (Login Required + Admin)                  │
└───────────────┬──────────────────────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────────────────────┐
│          CLICK: "Verifikasi" Menu                        │
└───────────────┬──────────────────────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────────────────────┐
│   GET /verifikasi - AdminDokumenController::index()     │
│   - Query all dokumens                                   │
│   - Apply filter if exists                              │
│   - Paginate results                                    │
└───────────────┬──────────────────────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────────────────────┐
│         SHOW: Verification Dashboard                     │
│  ┌──────────────────────────────────────────────────┐   │
│  │ Filter: [Semua] [Pending] [Disetujui] [Ditolak] │   │
│  └──────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────┐   │
│  │ TABLE: All Documents                             │   │
│  │ [Name] [DocType] [Status] [Date] [Actions]      │   │
│  │ Row 1  [Ijazah] [Pending] [3/1/26] [View][Verify]  │
│  │ Row 2  [NISN]   [Approved]                       │   │
│  │ ...                                              │   │
│  └──────────────────────────────────────────────────┘   │
└───────────────┬──────────────────────────────────────────┘
                │
    ┌───────────┴─────────────┐
    │                         │
    ▼                         ▼
┌─────────────┐       ┌─────────────────┐
│Click Filter │       │Click Row/Verify │
│             │       │                 │
│GET /?       │       │VIEW DOCUMENT    │
│status=...   │       │& OPTIONS        │
│             │       │                 │
│Reload with  │       │[Setujui Button] │
│filtered     │       │[Tolak Button]   │
│docs         │       │                 │
└─────────────┘       └────────┬────────┘
                               │
                    ┌──────────┴──────────┐
                    │                    │
                    ▼ SETUJUI            ▼ TOLAK
                ┌──────────┐         ┌──────────┐
                │PUT /dokumen/{id}/   │PUT /dokumen/...│
                │status                │status          │
                │                      │                │
                │status: disetujui     │Modal: Catatan  │
                │catatan: null         │[Form]          │
                │                      │[Submit]        │
                └─────────┬────────────┴────────┬───────┘
                          │                     │
                          ▼                     ▼
                ┌──────────────────┐  ┌──────────────────┐
                │UPDATE dokumens:  │  │UPDATE dokumens:  │
                │- status_verifikasi│ │- status_verifikasi
                │  = 'disetujui'    │ │  = 'ditolak'     │
                │- catatan = null   │ │- catatan = input │
                └─────────┬─────────┘  └─────────┬────────┘
                          │                     │
                          └──────────┬──────────┘
                                     │
                                     ▼
                        ┌──────────────────────┐
                        │RESPONSE: {success}   │
                        └──────────────────────┘
                                     │
                                     ▼
                        ┌──────────────────────┐
                        │SHOW SUCCESS MESSAGE  │
                        │RELOAD: Document     │
                        │status updated       │
                        └──────────────────────┘
```

---

## 📊 DATABASE RELATIONSHIPS

```
┌──────────────────┐
│     USERS        │
├──────────────────┤
│ id (PK)          │
│ name             │
│ email            │
│ password         │
│ role             │
│ is_verified      │
│ ...              │
└────────┬─────────┘
         │ 1
         │ hasMany
         │
         │ Many
         ▼
    ┌──────────────────────────┐
    │     DOKUMENS             │
    ├──────────────────────────┤
    │ id (PK)                  │
    │ user_id (FK) ─────┐      │
    │ nama_dokumen       │      │
    │ file_path          │      │
    │ file_type          │      │
    │ file_size          │      │
    │ status_verifikasi  │      │
    │ catatan_verifikasi │      │
    │ created_at         │      │
    │ updated_at         │      │
    │ deleted_at         │      │
    └────────────────────┘      │
         (Soft Delete)          │
```

---

## 🔄 STATE DIAGRAM - Dokumen Status

```
         ┌────────────────┐
         │  CREATED: NULL │
         └────────┬───────┘
                  │
                  │ SET DEFAULT
                  ▼
         ┌─────────────────────┐
         │ PENDING             │
         │ (Menunggu Verifikasi)│
         └────────┬────────────┘
                  │
      ┌───────────┴────────────┐
      │                        │
      │ ADMIN REVIEW           │
      │                        │
      ▼                        ▼
┌──────────────┐       ┌────────────┐
│ DISETUJUI    │       │ DITOLAK    │
│ (Approved)   │       │ (Rejected) │
│ ✓ Final      │       │ ✗ Final    │
└──────────────┘       └────────────┘
      │                       │
      │                       ▼ (User can reupload)
      │                  BACK TO PENDING
      │                       │
      └───────────┬───────────┘
                  │
                  ▼ (User Delete)
            SOFT DELETE
            (Audit Trail)
```

---

## 📂 FILE STORAGE STRUCTURE

```
storage/
└── app/
    └── public/
        └── dokumen/
            ├── 1704268800_ijazah.pdf
            ├── 1704268901_kk.jpg
            ├── 1704268902_akta.png
            └── ...
            
* File naming: {timestamp}_{original_filename}
* Format: PDF, JPG, JPEG, PNG
* Max size: 5MB
* Accessible via: storage_path('app/public/dokumen/...')
* Download via: route('dokumen.download', $id)
```

---

## 🔐 SECURITY FLOW

```
USER REQUEST
    │
    ▼ MIDDLEWARE
┌─────────────────┐
│ auth            │ → Check if logged in
│ web             │ → CSRF token
│ cekRole:user    │ → Check if user role
└────────┬────────┘
         │
         ▼ CONTROLLER
    ┌─────────────┐
    │ Validate    │
    │ File:       │
    │ - Type ✓    │ → MIME validation
    │ - Size ✓    │ → File size check
    │ - Content ✓ │ → Verify file exists
    └────────┬────┘
             │
             ▼ STORAGE
    ┌──────────────────┐
    │ Store File       │
    │ - Path: hidden   │ → Not directly accessible
    │ - Timestamp: add │ → Random naming
    │ - Permissions    │ → Limited access
    └────────┬─────────┘
             │
             ▼ DATABASE
    ┌──────────────────┐
    │ Log Action       │
    │ - File metadata  │ → For audit
    │ - User ID        │ → Ownership
    │ - Timestamps     │ → Soft delete
    └──────────────────┘
```

---

## 🚀 REQUEST/RESPONSE FLOW

### Upload Request
```
POST /dokumen
Headers:
  - X-CSRF-TOKEN: {token}
  - Content-Type: multipart/form-data

Body:
  - nama_dokumen: "Ijazah"
  - file: <binary>
```

### Upload Response - Success
```json
{
  "success": true,
  "message": "Dokumen berhasil diunggah",
  "data": {
    "id": 1,
    "user_id": 5,
    "nama_dokumen": "Ijazah",
    "file_path": "dokumen/1704268800_ijazah.pdf",
    "file_type": "pdf",
    "file_size": 2048576,
    "status_verifikasi": "pending",
    "created_at": "2026-01-03 10:00:00",
    "updated_at": "2026-01-03 10:00:00"
  }
}
```

### Upload Response - Error
```json
{
  "success": false,
  "message": "Validasi gagal",
  "errors": {
    "file": ["File terlalu besar"],
    "nama_dokumen": ["Field required"]
  }
}
```

---

## 📈 DATA FLOW DIAGRAM

```
┌─────────────────────┐
│   User Interface    │
│   (Blade Template)  │
└──────────┬──────────┘
           │ HTTP
           ▼
┌─────────────────────────┐
│   Laravel Routes        │
│   (web.php)             │
└──────────┬──────────────┘
           │
           ▼
┌─────────────────────────────┐
│   Controller                │
│   (DokumenController)       │
│   - Validate              │
│   - Process File          │
│   - Query Database        │
└──────────┬──────────────────┘
           │
      ┌────┴─────┐
      │           │
      ▼           ▼
┌──────────┐  ┌──────────────┐
│ Storage  │  │ Database     │
│ (Files)  │  │ (dokumens)   │
│          │  │              │
│ /dokumen │  │ SQL queries  │
│ /uploads │  │ Models/ORM   │
└──────────┘  └──────────────┘
      │           │
      └────┬──────┘
           │
           ▼
┌─────────────────────┐
│   JSON Response     │
│   {"success": ...}  │
└──────────┬──────────┘
           │ JavaScript
           ▼
┌─────────────────────┐
│   DOM Update        │
│   - Show message    │
│   - Refresh table   │
│   - Hide modal      │
└─────────────────────┘
```

---

## ⏱️ TIMING DIAGRAM

```
User Action                    Typical Response Time
───────────────────────────────────────────────────

Upload File (5MB)              1-3 seconds
│ ├─ File validation           100ms
│ ├─ File storage              800ms
│ ├─ DB insert                 200ms
│ └─ Response                  100ms
│
Download File (5MB)            2-4 seconds
│ ├─ DB query                  50ms
│ ├─ Auth check                50ms
│ ├─ File read                 500ms
│ └─ Transfer                  1000ms+
│
Delete Document                500ms
│ ├─ DB update (soft delete)   200ms
│ ├─ File deletion             100ms
│ └─ Response                  200ms
│
Admin List (1000 docs)         500-1000ms
│ ├─ DB query + join           300ms
│ ├─ Pagination                100ms
│ └─ Template render           200ms
│
Admin Approve                  300ms
│ ├─ Auth check                50ms
│ ├─ DB update                 150ms
│ └─ Response                  100ms
```

---

**Diagram Updated**: 3 Januari 2026
